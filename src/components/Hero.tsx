import React from "react"
import { Button } from "@/components/ui/button"
import { Button } from "@/components/ui/button"
export default function Hero() {


      opacity: 1,
        staggerChildren: prefersReducedMotion ? 0

  }
    hidden: { opacity: 0 },
    visible: {
      y: 0
  }
  const itemTransition = {
  }
  const
  }
  }

  const itemVariants: Variants = {
        className="space-y-6"
        <motio
            <Spar
          
    }
  }

            <span classNam
            </span>{" "}
   

  const handleExplore = () => {
            <span className="font-semibold text
  }

  return (
            className="w-full rounded-full bg-primary text-sm
            Explor
          </Button>
      </motion.div>
  )


























        </motion.div>






          >
            Explore Solutions


        </motion.div>

    </section>

}
